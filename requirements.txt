# 创建新的requirements.txt
cat > requirements.txt << 'EOF'
# Web框架
flask==2.3.3
flask-cors==4.0.0

# AI/图像处理
ultralytics==8.0.196
numpy==1.24.3
opencv-python-headless==4.8.1.78  # 不需要GUI支持，更稳定

# 数据处理
pandas==2.0.3  # 可选，用于数据分析
EOF